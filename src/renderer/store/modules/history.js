const state = {
	logs: {},
}

const getters = {
	allLogs: state => state.logs,
}

const mutations = {}

const actions = {}

export default {
	namespaced: true,
	state,
	getters,
	mutations,
	actions
}
