const state = {
	allRepository: []
};

const getters = {
	getAllRepository: state => state.allRepository
};

const mutations = {
	}
};

const actions = {};

export default {
	namespaced: true,
	state,
	getters,
	mutations,
	actions
};
