linter:
  goodcheck:
    config: goodcheck.yml
    target:
      - ".electron-vue"
      - "src"
  eslint:
    dir:
      - ".electron-vue"
      - "src"
    npm_install: true
    config: ".eslintrc.js"

ignore:
  - ".github/**"
  - ".vscode/**"
  - "static/**"
